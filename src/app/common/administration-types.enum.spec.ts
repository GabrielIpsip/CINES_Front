import { AdministrationTypesEnum } from './administration-types.enum';

describe('AdministrationTypesEnum', () => {
  it('should create an instance', () => {
    expect(new AdministrationTypesEnum()).toBeTruthy();
  });
});
