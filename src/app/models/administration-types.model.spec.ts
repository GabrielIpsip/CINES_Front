import { AdministrationTypes } from './administration-types.model';

describe('AdministrationTypes', () => {
  it('should create an instance', () => {
    expect(new AdministrationTypes()).toBeTruthy();
  });
});
