import { BroadcastDataType } from './broadcast-data-type.model';

describe('BroadcastDataType', () => {
  it('should create an instance', () => {
    expect(new BroadcastDataType()).toBeTruthy();
  });
});
