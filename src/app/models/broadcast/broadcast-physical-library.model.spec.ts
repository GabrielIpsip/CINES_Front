import { BroadcastPhysicalLibrary } from './broadcast-physical-library.model';

describe('BroadcastPhysicalLibrary', () => {
  it('should create an instance', () => {
    expect(new BroadcastPhysicalLibrary()).toBeTruthy();
  });
});
