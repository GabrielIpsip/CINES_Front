import { BroadcastInstitution } from './broadcast-institution.model';

describe('Institution', () => {
  it('should create an instance', () => {
    expect(new BroadcastInstitution()).toBeTruthy();
  });
});
