import { EstablishmentTypes } from './establishment-types.model';

describe('EstablishmentTypes', () => {
  it('should create an instance', () => {
    expect(new EstablishmentTypes()).toBeTruthy();
  });
});
