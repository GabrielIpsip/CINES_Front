import { GroupInstructions } from './group-instructions.model';

describe('GroupInstructions', () => {
  it('should create an instance', () => {
    expect(new GroupInstructions()).toBeTruthy();
  });
});
