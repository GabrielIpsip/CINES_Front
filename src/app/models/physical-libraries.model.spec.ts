import { PhysicalLibraries } from './physical-libraries.model';

describe('PhysicalLibraries', () => {
  it('should create an instance', () => {
    expect(new PhysicalLibraries()).toBeTruthy();
  });
});
