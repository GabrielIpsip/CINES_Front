import { PhysicalLibraryLinkHistory } from './physical-library-link-history.model';

describe('PhysicalLibraryLinkHistory', () => {
  it('should create an instance', () => {
    expect(new PhysicalLibraryLinkHistory()).toBeTruthy();
  });
});
