import { PhysicalLibraryTypes } from './physical-library-types.model';

describe('PhysicalLibraryStates', () => {
  it('should create an instance', () => {
    expect(new PhysicalLibraryTypes()).toBeTruthy();
  });
});
