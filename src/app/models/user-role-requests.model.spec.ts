import { UserRoleRequests } from './user-role-requests.model';

describe('UserRoleRequests', () => {
  it('should create an instance', () => {
    expect(new UserRoleRequests()).toBeTruthy();
  });
});
